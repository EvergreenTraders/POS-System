name: Deploy to AWS

on:
  # Manual deployment trigger - click "Run workflow" button in GitHub Actions
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging
      run_migrations:
        description: 'Run database migrations'
        required: true
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

  # Auto-deploy when pushing to deploy branch
  push:
    branches:
      - deploy
      # Uncomment below to auto-deploy on main branch merges
      # - main

env:
  AWS_REGION: ca-central-1
  EB_APPLICATION_NAME: pos-system
  EB_ENVIRONMENT_NAME: pos-system-prod
  FRONTEND_S3_BUCKET: pos-system-frontend
  NODE_VERSION: '18'

jobs:
  deploy-backend:
    name: Deploy Backend to Elastic Beanstalk
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install backend dependencies
        run: |
          cd backend
          npm install --production

      - name: Generate deployment package
        run: |
          zip -r deploy.zip backend/ .ebextensions/ .platform/ -x "*/node_modules/*" "*.git*"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Upload package to S3
        run: |
          aws s3 cp deploy.zip s3://elasticbeanstalk-${{ env.AWS_REGION }}-${{ secrets.AWS_ACCOUNT_ID }}/pos-system/deploy-${{ github.sha }}.zip

      - name: Create new Elastic Beanstalk version
        run: |
          # Check if version already exists
          if aws elasticbeanstalk describe-application-versions \
            --application-name ${{ env.EB_APPLICATION_NAME }} \
            --version-labels ${{ github.sha }} \
            --query 'ApplicationVersions[0]' \
            --output text | grep -q ${{ github.sha }}; then
            echo "Version ${{ github.sha }} already exists, skipping creation"
          else
            aws elasticbeanstalk create-application-version \
              --application-name ${{ env.EB_APPLICATION_NAME }} \
              --version-label ${{ github.sha }} \
              --source-bundle S3Bucket="elasticbeanstalk-${{ env.AWS_REGION }}-${{ secrets.AWS_ACCOUNT_ID }}",S3Key="pos-system/deploy-${{ github.sha }}.zip"
          fi

      - name: Deploy to Elastic Beanstalk
        run: |
          aws elasticbeanstalk update-environment \
            --application-name ${{ env.EB_APPLICATION_NAME }} \
            --environment-name ${{ env.EB_ENVIRONMENT_NAME }} \
            --version-label ${{ github.sha }}

      - name: Wait for deployment
        run: |
          aws elasticbeanstalk wait environment-updated \
            --application-name ${{ env.EB_APPLICATION_NAME }} \
            --environment-name ${{ env.EB_ENVIRONMENT_NAME }}

  migrate-database:
    name: Run Database Migrations
    runs-on: ubuntu-latest
    needs: deploy-backend
    if: ${{ github.event.inputs.run_migrations == 'true' }}

    steps:
      - name: Wait for backend to be ready
        run: |
          echo "Waiting for backend to be fully deployed and healthy..."
          sleep 60

      - name: Run migrations via API
        run: |
          echo "Running database migrations via API..."

          # Get the EB environment URL with region
          EB_URL="http://${{ env.EB_ENVIRONMENT_NAME }}.ca-central-1.elasticbeanstalk.com"

          echo "Calling migration endpoint at $EB_URL/api/migrate"

          # Call the migration endpoint with increased timeout
          RESPONSE=$(curl -X POST "$EB_URL/api/migrate" \
            -H "X-Migration-Key: ${{ secrets.MIGRATION_KEY }}" \
            -H "Content-Type: application/json" \
            -w "\nHTTP_STATUS:%{http_code}" \
            --max-time 300 \
            -s)

          # Extract HTTP status
          HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS" | cut -d':' -f2)
          BODY=$(echo "$RESPONSE" | sed '/HTTP_STATUS/d')

          echo "Response:"
          echo "$BODY" | jq '.' 2>/dev/null || echo "$BODY"

          # Check if successful
          if [ "$HTTP_STATUS" = "200" ]; then
            echo "✓ Database migrations completed successfully!"
          else
            echo "✗ Migration failed with HTTP status: $HTTP_STATUS"
            echo "Response body: $BODY"
            exit 1
          fi

  deploy-frontend:
    name: Deploy Frontend to S3
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm install

      - name: Build React app
        run: |
          cd frontend
          npm run build
        env:
          CI: false
          REACT_APP_API_URL: ${{ secrets.REACT_APP_API_URL }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy to S3
        run: |
          aws s3 sync frontend/build/ s3://${{ env.FRONTEND_S3_BUCKET }}/ --delete

      - name: Invalidate CloudFront cache (optional)
        continue-on-error: true
        run: |
          if [ -n "${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}" ]; then
            aws cloudfront create-invalidation \
              --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} \
              --paths "/*"
          else
            echo "CloudFront distribution ID not set, skipping cache invalidation"
          fi
